#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Guard: prevent non-critical .md files in root (keep docs/ organized)
bash scripts/validate-root-docs.sh true

# Block accidental secret commits before linting.
pnpm -s version:check-secrets

npx lint-staged
