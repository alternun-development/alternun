# Legitimate environment variable references in infrastructure config
# These are variable names being passed to configuration objects, not actual secrets

# Mobile app build script: EXPO_PUBLIC_SUPABASE_KEY is a public Supabase publishable key (sb_publishable_*)
# Not a secret - safe to commit as it's client-side only
apps/mobile/build.sh:generic-api-key:139
apps/mobile/build.sh:generic-api-key:159
