interface:
  display_name: "Alternun"
  short_description: "Repo-specific patterns and workflows for alternun"
  default_prompt: "Use the alternun repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true