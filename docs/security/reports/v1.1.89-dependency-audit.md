# v1.1.89 dependency audit

The release manifests update `packages/auth/package.json` from `1.1.87` to
`1.1.88` as part of the generated v1.1.88 release. That file has no dependency,
source, permission, or authentication-flow change in this range.

## Scanner result

Run on 2026-09-20 from the v1.1.88 development release checkout:

```text
pnpm security:audit
Security audit passed: checked 2521 packages, no critical vulnerabilities found.
```

The audit queries the npm bulk advisory endpoint for the packages resolved by the
lockfile and fails at the repository's `critical` severity gate. It does not claim
that lower-severity advisories are absent.

Audited lockfile SHA-256:

```text
4b0cb9cc683a21cf7c82a8d98c7294ed19d16c1719501c1d367715108223a15e
```

## Complementary CI gates

The generated production promotion retains the following required evidence for its
exact release head:

- dependency security audit;
- CodeQL JavaScript/TypeScript and Actions analysis;
- Gitleaks secret scanning; and
- the focused milestone-sharing authorization and public-metadata regression tests.

This record is release evidence. It does not alter authentication behavior or grant
client access to the milestone-sharing storage bucket.
