# Generated by @edcalderon/versioning workspace-env. Do not edit directly.
# Example env for target admin

# Public API base URL
VITE_API_URL=http://localhost:8082
# Admin app environment name
VITE_APP_ENV=development
# Admin auth audience
VITE_AUTH_AUDIENCE=alternun-app
# Local Authentik development client id
VITE_AUTH_CLIENT_ID=alternun-admin-dev
# Local Authentik issuer URL (the production deployment supplies its own issuer)
VITE_AUTH_ISSUER=http://127.0.0.1:9000/application/o/alternun-admin-dev/
# Optional branded Authentik login entrypoint for password-first admin sign-in.
VITE_AUTH_LOGIN_ENTRY_URL=
# Set true only after a Google OAuth source has been configured in this Authentik instance.
VITE_AUTH_GOOGLE_ENABLED=false
# Optional Authentik entry flow used to preserve the OIDC dashboard redirect through Google.
VITE_AUTH_GOOGLE_FLOW_SLUG=alternun-google-login
# Optional Authentik flow slug for hosted email/password sign-in when no full entry URL is configured.
VITE_AUTH_PASSWORD_FLOW_SLUG=
